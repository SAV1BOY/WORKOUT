import { EmConstrucao } from "@/components/em-construcao";

export const metadata = { title: "Barra fixa — Treino do Terraço" };

export default function BarraFixa() {
  return (
    <EmConstrucao
      titulo="Barra fixa"
      descricao="Plano de 12 semanas, sessão da semana e as repetições soltas do dia."
      marco={4}
    />
  );
}
